version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8001:8000"
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "8085:80"
    depends_on:
      - backend
